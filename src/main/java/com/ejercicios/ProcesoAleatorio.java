package com.ejercicios;

/**
 * The type Proceso aleatorio.
 */
public class ProcesoAleatorio {
    /**
     * Ejecutar boolean.
     *
     * @return the boolean
     */
    public boolean ejecutar() {
        // Simula un proceso que tiene un 50% de probabilidad de éxito
        return Math.random() < 0.5;
    }
}
